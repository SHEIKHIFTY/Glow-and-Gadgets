{
  "compilerOptions": {
    "baseUrl": "src",        // base folder for @ alias
    "paths": {
      "@/*": ["*"]           // @/ points to /src/*
    }
  },
  "exclude": ["node_modules", ".next"]
}
